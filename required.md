# Details needed to publish this assignment

Please provide the following:

1. **GitHub repository URL** — create an empty public repository first, then paste its HTTPS URL here or send it in chat. Do not add a README, `.gitignore`, or license during repository creation.
2. **Deployment URL** — after you deploy (for example, to Vercel), provide the public URL for `candidate.demo_url`.

The local Git identity is already configured as `AmankMeena289 <amankumarm289@gmail.com>`. Once the repository URL is provided, I will:

- initialise the local Git repository and connect it to GitHub;
- make an initial commit for the completed implementation;
- commit future meaningful changes as they are made; and
- push each commit to the configured GitHub repository.

Do not put passwords, API keys, personal access tokens, or other secrets in this file or in chat. GitHub authentication should be completed locally through Git Credential Manager or the GitHub CLI when prompted.
